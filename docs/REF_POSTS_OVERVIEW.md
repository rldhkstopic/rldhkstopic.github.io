# Posts Overview

이 문서는 블로그에 작성된 글 목록과 각 글의 핵심 내용을 한눈에 볼 수 있도록 정리한 종합 요약 파일이다. 새 글을 작성할 때마다 이 파일을 함께 업데이트하도록 한다.

## Daily

- **2024-01-25 · 스타일 가이드 - 블로그 포스트 서식 예시**  
  블로그 전반에서 사용하는 제목, 본문, 리스트, 코드, 인용, 표 등의 **시각 스타일과 마크다운 패턴을 카탈로그처럼 보여주는 레이아웃 가이드**다.

## Dev

- **2025-12-12 · MAS(Multi-Agent System) 학습: PM 관점에서 AI 에이전트 활용하기**  
  MAS 프레임워크(MetaGPT, AutoGen, CrewAI, Dify 등)를 비교하고, **PM 관점에서 Cursor를 중심으로 Clean Room 프로토콜을 활용해 멀티 에이전트를 운용하는 전략**을 정리한 글이다.

- **2025-12-16 · FPGA에서 플래시 메모리와 BRAM을 어떻게 써야 하는가**  
  FPGA 설계에서 플래시와 BRAM의 특성과 역할을 분리하고, **부트/이미지 저장소로서의 플래시와 실시간 연산 버퍼로서의 BRAM을 어떻게 계층 구조 안에 배치할지**를 고찰한 글이다.

- **2025-12-16 · FPGA에서 EEPROM 값을 BRAM/LUT로 로드하는 설계 고찰**  
  외부 EEPROM에 저장된 설정값을 **부팅 시점에 한 번만 읽어 BRAM/LUT에 로드한 뒤 런타임에는 온칩 메모리만 참조하는 구조**를 FSM, 데이터 포맷, 에러 처리 관점에서 정리한 글이다.

- **2025-12-16 · EEPROM(Flash) 컨트롤러와 BRAM 사이의 데이터 경로 설계**  
  **8-bit 직렬 스트림을 16-bit 병렬 워드로 변환하여 BRAM에 저장하는 파이프라인 구조**를 VHDL 코드와 함께 설명한다. SPI Controller, Data Assembler, Block RAM으로 구성된 3단계 파이프라인의 구현 방법과 타이밍 특성을 다룬다.

- **2026-01-08 · VHDL 문법 정리 (1): 설계 단위와 기본 구조(entity/architecture)**  
  `entity/architecture/package`를 **설계 단위 관점**에서 정리하고, 파일 구조/라이브러리 관리에 영향을 주는 기본 원칙을 정리한 글이다.

- **2026-01-08 · VHDL 문법 정리 (2): 타입/신호/변수, 동시·순차 문장, 프로세스**  
  `std_logic_vector`와 `unsigned/signed`의 역할을 분리하고, `signal`/`variable`의 **업데이트 시점 차이**를 시간 모델 관점에서 설명한 글이다.

- **2026-01-08 · VHDL 문법 정리 (3): 클럭/리셋 템플릿, generate/generic, 구조 확장**  
  합성 가능한 클럭/리셋 템플릿을 고정하고, `generate/generic`으로 **구조를 확장/선택**하는 패턴을 예제로 정리한 글이다.

- **2026-01-08 · VHDL 문법 레퍼런스: 설계 단위부터 프로세스/타입/generate까지(단일 포스트)**  
  VHDL-2008 기준으로 `entity/architecture/package`, 타입/선언, 동시·순차 문장, `generate`, 함수/프로시저, 속성까지를 **문법 단위로 쪼개어** 빠르게 찾아보는 참고서 형태로 정리한 글이다.

## Document

- **2025-12-13 · 연준 정책과 업스타트 홀딩스 영향 분석**  
  연준의 QT 중단, 레포 시장, QRA, 엔캐리 트레이드 구조를 정리한 뒤, **이 거시 환경이 업스타트 홀딩스의 펀더멘털과 밸류에이션에 어떻게 작용하는지**를 데이터와 리포트 인용을 통해 분석한 글이다.


